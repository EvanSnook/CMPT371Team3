using System;
using System.Collections.Generic;
using System.Threading;
using NUnit.Framework;
using UnityEngine;

namespace UnityTest
{
	[TestFixture]
	[Category("Unit Tests")]
	internal class ResizeSliderTests
	{
		[Test]
		public void updateImageTest()
		{

			// TDD: Scripts are not fully developed yet

//			GameObject image = new GameObject ();
//			float scaleSize = image.transform.localScale.x;
//			image.AddComponent<ResizeSlider>();
//			ResizeSlider slider = image.GetComponent<ResizeSlider> ();
//
//			slider.image = image;
//			slider.scaleSize = 2.0F;
//
//			slider.updateImage();
//
//			Assert.AreEqual (slider.scaleSize, image.transform.localScale.x);
//			Assert.AreNotEqual (scaleSize, image.transform.localScale.x);

			Assert.Pass();
		}
	}
}
